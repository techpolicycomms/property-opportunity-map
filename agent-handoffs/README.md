# Agent handoffs

Structured notes passed between agent roles (see `AGENTS.md` §3.3).

- Use `TEMPLATE.md` for new notes: `YYYYMMDD-<role>-<topic>.md`.
- Append one line per note to `INDEX.md`.
- A handoff states: what changed, what was verified, what is known-broken,
  what the next role should do.
